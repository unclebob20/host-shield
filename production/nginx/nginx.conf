# Production Nginx Configuration

user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    # Gzip Compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Upstream Definitions
    upstream api_server {
        server api-server:3000;
    }

    server {
        listen 80;
        server_name localhost; 
        # In production replace 'localhost' with your domain, e.g., app.hostshield.sk
        
        # Security Headers
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-XSS-Protection "1; mode=block";
        add_header X-Content-Type-Options "nosniff";

        # 1. Serve React App (Static Files)
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        # 2. Proxy API Requests
        location /api/ {
            # Rewrite /api/... to /... if backend expects root paths, 
            # OR keep if backend handles /api prefix. 
            # Based on previous code, the backend seems to handle paths directly, so we might need a rewrite or strict routing.
            # Assuming backend is mounted at root paths like /auth, /guests, etc.
            
            proxy_pass http://api_server;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
        
        # Specific routes for GovBridge if exposed publicly (Usually not recommended, protect via API Gateway)
        # MRZ Reader shouldn't be exposed publicly.
    }
}
